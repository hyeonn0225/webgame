# LAST SIGNAL — 2인 PvP 배틀로얄 구현 사양

이 문서는 기존 PC방 협동 생존 기획을 대체하는 AI 에이전트용 최종 사양이다.

## 목표와 흐름

각자 컴퓨터에서 Vercel 사이트에 접속하고 Supabase Realtime으로 같은 방에 참가한다. 온라인 플레이어는 정확히 2명이다.

로비 → 20초 착지 위치 선택 → 3초 강하 → 파밍과 수동 PvP → 최후의 1인 승리 → 방장 재시작 순서다. 미선택 플레이어는 유효한 기본 위치에 배치한다. 선택한 착지 좌표는 상대에게 공개하지 않는다.

## 전장

- 2400×2400 유한 전장. 자기장으로 교전을 유도한다.
- 건물 16채, 부쉬, 십자 도로, 지상 전리품.
- 건물 아래쪽 중앙 50px 구간은 출입구이며 다른 벽은 이동과 탄환을 막는다.
- 건물에 들어가면 지붕을 숨기고 내부를 그린다. 나가면 지붕이 다시 덮인다.
- 부쉬는 안에 들어가면 반투명으로 표시한다.
- 상대와 아이템이 건물/부쉬 내부에 있으면 관찰자가 같은 공간에 있을 때만 표시하고 좌표를 전송한다.
- 미니맵과 M 전술 지도에는 자기 위치, 지형, 현재/다음 안전 구역을 표시한다. 상대 위치는 표시하지 않는다.

## 전투 및 아이템

자동 조준과 자동 공격, 경험치, 협동 부활은 제거한다.

| 총기 | 탄약 | 탄창 | 피해 | 발사 간격 | 장전 시간 |
|---|---|---:|---:|---:|---:|
| P92 | 9mm | 12 | 24 | 0.32초 | 1.2초 |
| VECTOR | 9mm | 24 | 16 | 0.095초 | 1.6초 |
| M416 | 5.56mm | 30 | 28 | 0.16초 | 2초 |

- 시작 체력 100, 맨손, 탄약 0.
- F로 65px 이내 가장 가까운 보이는 전리품 획득. 총기 슬롯은 1개이며 교체 시 이전 총과 남은 탄창을 떨어뜨린다.
- 새 총의 탄창은 비어 있다. 탄약을 따로 파밍한 후 R로 장전한다.
- 마우스 조준, 좌클릭 유지 시 발사 주기에 맞춰 연사한다. 매 발 탄창 1 감소.
- 탄창이 비거나 장전/치료 중이면 발사하지 않는다.
- 총알을 짧은 구간으로 나누어 충돌 판정해 빠른 탄환의 벽 통과를 방지한다.
- 회복제는 Q로 2.5초 사용하고 체력 60 회복. 치료 중 느리게 이동하며 피격 시 취소된다.
- 자기 탄환은 자신에게 피해를 주지 않는다. 상대 체력이 0이면 탈락한다.

## 자기장

중심은 (1200,1200). 초기 반경 1650에서 45초 준비 후 5단계로 1050 → 650 → 320 → 120 → 0까지 축소한다. 각 단계는 대기 20초 + 축소 40초다. 밖에서는 단계별 초당 3/6/9/12/15 피해를 받는다. 반경 0에서는 중심에 있어도 피해를 받는다. 최종적으로 한 명 생존하면 승리, 동시 사망은 무승부다.

## 구현 파일과 동기화

- battle.js: 지형, 이동/충돌, 전리품, 탄약, 전투, 자기장, 시점별 상태 생성.
- network.js: Supabase 채널, 방 참가/거절, 입력과 동작 전송, 방장 시뮬레이션.
- game.js: 조작, 화면 상태, Canvas 렌더링, 지도와 HUD.
- motion.js: 수신 상태 사이의 좌표와 방향 보간. 참가자 버퍼 90ms, 방장 버퍼 40ms. 은폐된 상대의 이전 좌표는 즉시 제거한다.
- index.html: UI 골격 및 스타일.
- build.js / vercel.json / DEPLOY.md: 정적 배포.
- test.js / browser-test.cjs: 규칙 및 두 페이지 통합 검증.

방장이 초당 30회 계산하고 초당 15회 참가자용 상태를 전달한다. 참가자의 행동 메시지는 증가하는 번호로 중복 적용을 막고, 매치 번호가 다른 파밍/착지 요청은 무시한다. 게임 시작은 방장만 가능하다. 두 사람의 연결이 유지될 때만 진행한다. 일시정지와 재접속 후 재개는 착지/강하/전투 중 원래 단계로 돌아온다.

공개 채널 + 방장 계산 방식이므로 인증된 비공개 매치나 방장 치트 방지는 범위 밖이다. 탭 종료 후 복구, 방장 이전, 다인 매칭, 영구 인벤토리는 지원하지 않는다.

## 완료 검증

방 코드 참가와 2인 제한, 착지 선택 및 시간 초과, 빈 탄창 파밍/장전, 수동 사격과 탄약 소모, 벽 충돌, 건물·부쉬 은폐, 자기장 피해, 승패, 재시작을 확인한다. 두 브라우저 로컬 중계 테스트와 실제 Supabase 배포 검증을 구분하여 보고한다.
