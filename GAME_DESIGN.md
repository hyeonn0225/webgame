# PC방 생존자: 무한 야간근무

## 온라인 2인 구현 변경 사항 (기존 단일 파일 사양보다 우선)

최종 구현은 Vercel 정적 호스팅 + Supabase Realtime Broadcast를 사용한다. 아래 Node 상시 서버 방식은 대체되며 방장 브라우저가 전투를 계산한다. 방장 탭 종료·새로고침 후 복구는 지원하지 않는다. DEPLOY.md가 최종 배포 기준이다.

- 각자 컴퓨터에서 같은 서버 주소로 접속하고 6자리 방 코드로 참가한다.
- Node.js 서버가 이동·전투·성장을 판정하고 두 브라우저에 상태를 전달한다.
- 결과물은 `index.html`, `server.js`, `package.json`이며 외부 패키지는 사용하지 않는다.
- 2명이 접속하면 방장이 시작한다. 각자 WASD 또는 방향키를 사용한다.
- 카메라는 각자의 캐릭터를 추적하며, 화면 밖 친구의 방향을 표시한다.
- 경험치와 강화는 팀 공유다. 한 명이 강화 선택 시 둘 모두에게 적용한다.
- 체력은 개별이며, 쓰러진 동료 곁에서 3초 머물면 체력 50%로 구조한다. 둘 다 쓰러지면 패배한다.
- 10분 생존 또는 8분에 등장하는 보스 처치로 승리한다. 2인용으로 적 생성 빈도와 보스 체력을 조정한다.
- 한 명의 일시정지가 팀 전체에 적용된다. 연결 단절 시 정지하고 재연결 후 수동으로 재개한다.
- 실행 및 다른 컴퓨터의 접속 방법은 `README.md`를 따른다. 공개 인터넷 배포는 별도 작업이다.

## 문서 목적

이 문서는 AI 코딩 에이전트가 별도 질의 없이 플레이 가능한 웹게임 프로토타입을 구현하기 위한 작업 사양이다.

## 개발 목표

PC방에서 잠깐 즐길 수 있는 무한 맵 생존 게임을 구현한다.

- 한 판 플레이 시간: 최대 10분
- 3판 기준 약 30분 플레이 가능
- 설치, 로그인, 서버, 외부 라이브러리 없이 실행
- 구현 목표: 30분~1시간 내 동작하는 프로토타입
- 장르: 탑다운 2D 생존 액션

## 기술 사양

- 결과물: `index.html` 1개
- 기술: HTML, CSS, Vanilla JavaScript
- 렌더링: HTML5 Canvas 2D
- 외부 이미지, 폰트, 라이브러리 사용 금지
- 캐릭터, 몬스터, 맵은 Canvas 도형으로 표현
- 브라우저에서 `index.html`을 열면 즉시 실행
- 데스크톱 화면을 기준으로 하며 반응형 모바일 대응은 이번 범위에서 제외

## 핵심 플레이

플레이어는 끝없이 이어지는 PC방을 이동하며 몰려오는 감염된 손님을 피한다. 캐릭터는 가장 가까운 적을 자동으로 공격한다. 시간이 지날수록 적이 강해지며, 10분 동안 생존하면 승리한다.

### 플레이 흐름

1. 시작 화면에서 `게임 시작`을 클릭한다.
2. WASD 또는 방향키로 플레이어를 이동한다.
3. 무기는 가장 가까운 적을 향해 자동 발사한다.
4. 적을 처치하면 경험치 구슬이 나온다.
5. 경험치가 가득 차면 강화 선택창을 띄운다.
6. 적과 접촉하면 체력이 감소한다.
7. 10분 생존 시 승리 화면을 표시한다.
8. 체력이 0이 되면 게임오버 화면을 표시한다.
9. `다시 시작` 버튼으로 즉시 새 게임을 시작한다.

## 조작

| 입력 | 동작 |
|---|---|
| `WASD` / 방향키 | 플레이어 이동 |
| 마우스 | UI 버튼 클릭 |
| `1`, `2`, `3` | 강화 선택 |
| `P` | 일시정지 및 재개 |

## 화면 구성

### 시작 화면

- 제목: `PC방 생존자: 무한 야간근무`
- 짧은 설명: `10분 동안 살아남아라`
- `게임 시작` 버튼
- 조작 안내
- 최고 생존 시간 표시

### 게임 화면

- 좌측 상단: 체력 바
- 상단 중앙: 생존 시간 / 목표 시간 `10:00`
- 우측 상단: 처치 수
- 하단 중앙: 현재 레벨과 경험치 바
- 중앙: 플레이어와 주변 맵

### 결과 화면

- 승리 또는 게임오버 문구
- 생존 시간
- 처치 수
- 도달 레벨
- 최고 기록 갱신 여부
- `다시 시작` 버튼

## 무한 맵 구현 방식

실제 거대한 맵을 만들지 않는다. 월드 좌표와 카메라 오프셋을 사용해 무한 맵처럼 보이게 한다.

- 플레이어 위치는 월드 좌표로 관리한다.
- 카메라는 항상 플레이어를 화면 중앙에 둔다.
- 바닥은 고정 크기 타일을 반복해서 그린다.
- 타일 크기는 `64px`로 한다.
- 바닥 타일에는 어두운 회색 바닥과 연한 격자선을 표시한다.
- 월드 좌표를 타일 크기로 나눈 나머지를 사용해 타일을 반복 렌더링한다.
- 장애물은 이번 프로토타입에서 선택 사항이며, 구현 시간이 부족하면 제외한다.
- 적은 플레이어 주변 일정 거리 밖에서 생성하고, 너무 멀어진 적은 삭제한다.

## 게임 객체

### 플레이어

- 색상: 청록색
- 모양: 반지름 16px 원
- 이동 속도: 초당 220px
- 최대 체력: 100
- 피격 무적 시간: 0.5초
- 시작 공격력: 20
- 공격 주기: 0.6초
- 공격 사거리: 420px

### 기본 무기

- 자동 조준
- 가장 가까운 적을 타깃으로 선택
- 작은 노란색 탄환을 발사
- 탄환 속도: 초당 500px
- 적중 시 적에게 공격력만큼 피해
- 탄환은 적중 후 제거

### 일반 감염 손님

- 색상: 빨간색
- 모양: 반지름 12px 원
- 체력: 40
- 이동 속도: 초당 70px
- 접촉 피해: 초당 15
- 처치 시 경험치: 10

### 빠른 감염 손님

- 3분부터 생성
- 색상: 주황색
- 모양: 반지름 9px 원
- 체력: 25
- 이동 속도: 초당 130px
- 접촉 피해: 초당 20
- 처치 시 경험치: 15

### 보스

- 8분부터 등장
- 색상: 보라색
- 모양: 반지름 32px 원
- 체력: 1,500
- 이동 속도: 초당 45px
- 접촉 피해: 초당 30
- 처치 시 즉시 승리 처리
- 보스가 등장해도 10분 생존 조건은 유지한다.

## 적 생성 규칙

- 게임 시작 시 일반 적 3마리를 생성한다.
- 플레이어 중심 반경 500~700px 위치에서 생성한다.
- 화면 안에서 갑자기 나타나지 않도록 한다.
- 최대 적 수는 100마리로 제한한다.
- 0~2분: 1.0초마다 일반 적 1마리
- 2~5분: 0.7초마다 일반 적 1마리
- 5~8분: 0.5초마다 일반 적 1마리, 빠른 적 15% 확률
- 8~10분: 0.35초마다 적 생성, 빠른 적 30% 확률
- 보스는 8분 시점에 한 번만 생성한다.

## 경험치와 강화

- 적이 죽으면 경험치 구슬을 생성한다.
- 경험치 구슬은 플레이어가 80px 안에 들어오면 자동으로 끌려온다.
- 레벨업에 필요한 경험치: `현재 레벨 × 50`
- 레벨업 시 게임을 일시정지하고 강화 3개를 보여준다.
- 강화 선택은 버튼 클릭 또는 숫자키로 처리한다.

### 강화 목록

| 강화 | 효과 |
|---|---|
| 공격력 강화 | 공격력 +10 |
| 연사 강화 | 공격 주기 15% 감소 |
| 이동 속도 강화 | 이동 속도 +25 |
| 최대 체력 강화 | 최대 체력 +20, 현재 체력 +20 |
| 회복 강화 | 즉시 체력 25 회복 |
| 관통 탄환 | 탄환이 적 1명을 관통 |

강화 선택지는 목록에서 무작위로 3개를 중복 없이 보여준다. `관통 탄환`은 한 번 선택하면 다시 나오지 않아도 된다.

## 난이도와 승패

- 생존 시간이 매 초 증가한다.
- 생존 시간이 `600초`가 되면 승리한다.
- 플레이어 체력이 0 이하가 되면 게임오버다.
- 일시정지 중에는 모든 게임 로직을 멈춘다.
- 브라우저 탭이 비활성화되어도 게임 시간이 비정상적으로 진행되지 않도록 실제 프레임 경과 시간을 제한한다.

## 저장 데이터

`localStorage`에 다음 값만 저장한다.

- 최고 생존 시간
- 최고 처치 수
- 최고 레벨

저장 실패가 발생해도 게임 플레이는 계속되어야 한다.

## 구현 우선순위

### 필수

- Canvas 렌더링
- 플레이어 이동
- 카메라 및 무한 바닥
- 적 추적 이동
- 자동 공격
- 충돌 및 체력
- 타이머
- 게임오버와 승리
- 다시 시작

### 가능하면 추가

- 경험치 구슬
- 레벨업 강화 선택
- 빠른 적
- 보스
- 최고 기록 저장
- 간단한 화면 흔들림 효과

### 구현하지 않음

- 온라인 멀티플레이
- 로그인과 서버
- 인벤토리
- 상점과 결제
- 스토리 컷신
- 외부 에셋 다운로드
- 복잡한 애니메이션
- 모바일 터치 조작

## 완료 조건

다음 조건을 모두 만족하면 프로토타입 완료로 판단한다.

- `index.html`을 브라우저에서 열었을 때 오류 없이 시작 화면이 표시된다.
- 게임 시작 후 WASD 또는 방향키로 플레이어가 이동한다.
- 플레이어가 이동해도 바닥이 끝없이 이어지는 것처럼 보인다.
- 적이 플레이어를 추적한다.
- 플레이어가 자동으로 적을 공격한다.
- 적과 접촉하면 체력이 감소한다.
- 적을 처치하면 처치 수가 증가한다.
- 10분 생존 또는 체력 0에서 결과 화면이 표시된다.
- 다시 시작 버튼이 정상 동작한다.
- 화면 크기 변경 시 Canvas가 게임 영역에 맞춰진다.
- 개발자 콘솔에 오류가 남지 않는다.

## 에이전트 구현 지시

먼저 `index.html`을 생성하고, 위 기능을 한 파일 안에 구현한다. 외부 패키지를 설치하지 않는다. 기능을 추가하기 전에 필수 기능이 모두 동작하는지 확인한다. 그래픽은 도형과 텍스트만 사용하며, 게임이 시작되자마자 조작 방법을 화면에 표시한다. 코드에는 `GameState`, `update()`, `render()`, `spawnEnemy()`, `restartGame()`처럼 역할이 분명한 함수와 상태를 사용한다.
